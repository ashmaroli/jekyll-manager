#!/bin/sh

set -e

echo "Running Ruby tests..."
RACK_ENV=test JEKYLL_LOG_LEVEL=debug bundle exec rspec --format documentation

# Appveyor doesn't seem to like Rubocop for some reason
if [ -z "$APPVEYOR" ]; then
  script/fmt
fi
